spring.application.name=AbsenceManager

server.port=${PORT:8080}
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:sqlserver://AbsenceManager.mssql.somee.com:1433;databaseName=AbsenceManager;integratedSecurity=false;encrypt=true;trustServerCertificate=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:Castrinho}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:12345678}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME:com.microsoft.sqlserver.jdbc.SQLServerDriver}
spring.jpa.database-platform=${SPRING_JPA_DATABASE_PLATFORM:org.hibernate.dialect.SQLServerDialect}
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.properties.hibernate.show_sql=true
server.servlet.session.cookie.same-site=none
server.servlet.session.cookie.secure=true

#spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
#spring.jpa.hibernate.naming.implicit-strategy=org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl

# Algumas explica��es

# integratedSecurity: (true) Indica que a conex�o ser� feita com as credenciais do Windows (false) Credencial do SQL Server
# encrypt: (true) Requer criptografia do Cliente (false) N�o requer criptografia do cliente
# trustServerCertificate: (true) instrui o aplicativo cliente a ignorar a valida��o do certitificado SSL/TSL do Servidor

#spring.jpa.hibernate.ddl-auto:
# none: Banco de dados e tabelas dever ser criados com anteced�ncia
# update: Banco de dados criado com anteced�ncia, tabelas podem ser criadas com anteced�ncia, as tabelas s�o atualizadas
#         pelo c�digo (model) toda vez que vc altera as entidades
# create-drop: Banco de dados deve ser criado com anteced�ncia, as tabelas s�o criadas automaticamente pelo c�digo (model ),
#              entretanto, as tabelas s�o exclu�das toda vez que o servidor � "encerrado"
# create: Banco de dados deve ser criado com anteced�ncia, toda vez que o servidor � "carregado" as tabelas s�o recriadas

# spring.jpa.show-sql= (true) As intera��es (DDL, DML) com o banco de dados s�o exibidas no console
#                      (false) As intera��es (DDL, DML) com o banco de dados n�o ser�o exibidas no console
